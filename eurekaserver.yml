server:
  port: 8080

eureka:
  instance:
    hostname: loclahost
  client:
    fetchRegistry: false
    regisertWithEureka: false
    servicerUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
