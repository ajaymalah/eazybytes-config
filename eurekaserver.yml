server:
  port: 8080

eureka:
  instance:
    hostname: loclahost
  client:
    fetchRegistry: false
    registertWithEureka: false
    servicerUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
